<!DOCTYPE html>
<html>
<head>
  <title>Gorilla</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <h3>Gorilla</h3>
    <div id="header_links">
      <%= yield :links %>
    </div>
  </header>
  <div class="notification" id="notice" ><%= notice %></div>
  <div class="notification" id="alert"><%= alert %></div>
  <article>
    <%= yield %>
  </article>
  <% if user_signed_in? %>
    <footer>
      <div id="user_nav">
        Signed in as: <%= current_user.email %>.
        <%= link_to "Sign out", destroy_user_session_path, 
                                method: "DELETE",
                                id: :top_nav_sign_out %>
      </div>
    </footer>
  <% end %>
</body>
</html>

